;; Define the workspaces widget
(defwidget workspaces []
  (box :class "box_workspaces"
       :orientation "h"
       :halign "center"
       :valign "center"
       :space-evenly true
       (literal :content workspace)))