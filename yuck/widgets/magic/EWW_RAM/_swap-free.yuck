(defwidget swap_free []
  (base_resource_circular
    :icon "󰍛"
    :tooltip "Swap Free: ${round((EWW_RAM.free_swap / EWW_RAM.total_swap) * 100, 0)}%"
    :value {((EWW_RAM.free_swap / EWW_RAM.total_swap) * 100)}
    :label "${round((EWW_RAM.free_swap / EWW_RAM.total_swap) * 100, 0)}%"
    :class "swap_free"))